
name: wa_nux_app
description: A simple multi-platform WebView app for https://wa.nux.my.id/
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0
  webview_flutter_wkwebview: ^3.12.0
 webview_windows: ^0.4.0

flutter:
  uses-material-design: true
